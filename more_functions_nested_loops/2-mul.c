/**
 *mul- multiplies two ints and retruns the result
 *
 * @a: first int to multiply
 * @b: second int to multiply
 * Return: returns the result of a * b
 */
int mul(int a, int b)
{
	return (a * b);
}
