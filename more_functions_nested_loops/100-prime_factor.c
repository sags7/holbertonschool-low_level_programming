#include <stdio.h>
/**
 * main- program entry point. determines and prints the largest prime factor
 * of 612852475143 followed by an new line
 */
int main(void)
{
	int value = 612852475143, factor = 0, x =2;
	bool found = false;

	while (!found)
	{
		if(value % x == 0)
		{

		}
	    x++;
	}
}
